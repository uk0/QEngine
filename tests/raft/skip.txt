# Scenarios currently skipped while Raft is being implemented.  Entries
# get removed as scenarios become stable in the docker harness.
#
# Empty → all live scenarios are expected to pass on every run.
